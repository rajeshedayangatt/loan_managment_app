## Usage

### Installation

Install the dependencies

```sh
$ composer install
```

Generate project key 

```sh
$ php artisan key:generate
```

migrate database

```sh
$ php artisan migrate
```

seed database

```sh
$ php artisan db:seed
```